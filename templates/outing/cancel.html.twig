{% extends 'base.html.twig' %}

{% block title %}Annuler sortie{% endblock %}


{% block body %}
    <h1>Annuler une sortie</h1>
    <div>
        <p>Nom de la sortie : {{ outing.name }}</p>
        <p>Date de la sortie : {{ outing.dateBegin|date("d.m.Y") }}</p>
        <p>Ville organisatrice : TO DO</p>
        <p>Lieu : {{ userSite.name }}</p>
        {{ form_start(outingCancellationForm) }}
        {{ form_widget(outingCancellationForm) }}
        {{ form_end(outingCancellationForm) }}
        <a href="{{ path('outing') }}" class="button">Annuler</a>



    </div>
{% endblock %}
