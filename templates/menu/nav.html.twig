<nav class="navbar">

    <div class="nav-logo">
        <h1 class="logo">
            <a href="{{ path('outing') }}" >Sortir.com</a>
        </h1>
    </div>
    <div class="nav-items nav-items-phone">

            <a href="{{ path('outing') }}" class="nav-btn nav-item" id="nav-home">
                Accueil
            </a>

        <div class="nav-profile-container nav-item">
            <a href="{{ path('participant_profile_update') }}" class="nav-profile-btn" id="nav-profile">
                {{ app.user.firstName[:1] }}
            </a>

            <div class="dropdown">
                <div class="dropdown-arrow"><img src="{{ asset('icon/Stroke 1.png') }}"></div>
                <div class="dropdown-content">
                    <a href="{{ path('app_logout') }}">Se déconnecter</a>
                    <a href="{{ path('participant_profile_update') }}">Modifier mon profil</a>
                    {% if  is_granted('ROLE_ADMIN')  %}
                    <a href="{{ path('app_register') }}">Ajouter des utilisateurs</a>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</nav>